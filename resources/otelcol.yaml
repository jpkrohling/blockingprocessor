receivers:
  otlp:
    protocols:
      grpc:

exporters:
  logging/1:
  logging/2:

processors:
  blocking:

extensions:

service:
  extensions:
  pipelines:
    traces/1:
      receivers: [otlp]
      processors: [blocking]
      exporters: [logging/1]

    traces/2:
      receivers: [otlp]
      processors:
      exporters: [logging/2]
